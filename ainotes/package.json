{
  "name": "ainotes",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,green \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "start": "concurrently -n server,web -c cyan,green \"npm run start --workspace server\" \"npm run preview --workspace web\"",
    "lint": "echo 'No root lint'"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}

